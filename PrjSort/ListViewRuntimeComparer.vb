Public Class ListViewRuntimeComparer
    Implements IListViewRuntimeComparer

    Public Function Compare(x As Object, y As Object, ListViewItem) As Object Implements IListViewRuntimeComparer.Compare
        Throw New NotImplementedException()
    End Function
End Class
